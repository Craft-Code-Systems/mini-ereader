# KiCad backups / autosave / caches
*-backups/
*.bak
*.kicad_sch-bak
*.kicad_pcb-bak
_autosave-*
fp-info-cache
*.kicad_prl

# Local project state (per-user; regenerated by KiCad)
# NOTE: mini-ereader.kicad_prl is intentionally ignored — it holds
# per-user window/layer state, not design data.

# Fabrication outputs (regenerate from the design)
fab/
gerbers/
*.zip

# 3D render scratch
*.wrl
*.step-bak

# Python
__pycache__/
*.pyc
